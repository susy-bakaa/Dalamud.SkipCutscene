namespace SkipCutscene
{
    public enum GroupingSize
    {
        Solo,
        LightParty,
        FullParty,
        Alliance
    }

    public enum SkipBehaviour
    {
        Manual,
        Automatic,
        Permanent
    }
}
